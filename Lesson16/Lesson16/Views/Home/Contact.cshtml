@{
    ViewBag.Title = "Contact";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

<input type="button" onclick="getRates()" value="Показать курсы" />
<script type="text/javascript">
function getRates() {
    // AJAX - Async JS and XML

    $.ajax({
        method: "post",
        url: "@Url.Action("GetRatesHtml")",
        success: function (data) {
            $("#rates").html(data);
        }
    });

    @*$.ajax({
        method: "post",
        url: "@Url.Action("GetRatesJson")",
        success: function (data) {
            //var rates = JSON.parse(data);
            var rates = $.parseJSON(data);
            for (var i = 0; i < rates.length; i++) {
                alert([rates[i].Currency, rates[i].Rate])
            }
        }
    });*@

}
</script>
<div id="rates">Здесь будут курсы</div>

<address>
    One Microsoft Way<br />
    Redmond, WA 98052-6399<br />
    <abbr title="Phone">P:</abbr>
    425.555.0100
</address>

<address>
    <strong>Support:</strong>   <a href="mailto:Support@example.com">Support@example.com</a><br />
    <strong>Marketing:</strong> <a href="mailto:Marketing@example.com">Marketing@example.com</a>
</address>

